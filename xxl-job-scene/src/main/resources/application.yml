spring:
  application:
    name: xxl-job-scene
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://100.64.0.8:3306/muc_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

xxl:
  job:
    accessToken: felix-xxl-job
    admin:
      # admin地址
      addresses: http://127.0.0.1:8080/xxl-job-admin
    executor:
      # 执行器名称
      appName: xxl-job-scene
      # 执行器端口
      port: 9996
      # 执行器日志路径
      logPath: ./logs/xxl-job-scene
server:
  port: 9902
